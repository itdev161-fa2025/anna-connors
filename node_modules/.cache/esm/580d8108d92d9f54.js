let mongoose,config;_d92‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d92‍.w("config",[["default",["config"],function(v){config=v}]]);


//Get the connection string
const db = config.get('mongoURI');

//Connect to MongoDB
const connectDatabase = async () => {
    try {
        await mongoose.connect(db, {
            useUnifiedTopology: true
        });
        console.log('Connected to MongoDB');
    } catch (error) {
        _d92‍.g.console.log(error.message);

        //Exit with failure code
        process.exit(1);
    }
};

