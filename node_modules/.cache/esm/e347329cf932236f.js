let express,connectDatabase;_f93‍.w("express",[["default",["express"],function(v){express=v}]]);_f93‍.w("./config/db",[["default",["connectDatabase"],function(v){connectDatabase=v}]]);


// Initialize express applicaiton
const app = express();

//connect database
connectDatabase();

//API endpoints
app.get('/', (req, res) =>
    res.send('http get request sent to root api endpoint')
);

//connection listener
app.listen(3000, () => console.log('Express server running on port 3000'));
